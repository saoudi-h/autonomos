# WORKLOG: [BUG-02] Publish patch releases via changesets

**Date:** 2026-06-18
**Agent Status:** Complete

## 📝 Description

Cut and publish the patch releases for `@autonomos/cli@0.3.1` and `@autonomos/core@0.3.1` that carry the BUG-01 fixes. No protocol version bump — this is a CLI bugfix, not a protocol change.

## 🛠 Flow

1. Committed BUG-01 in three Conventional Commits on `main`:
   - `feat(core): add findProjectRoot, isSamePath, displayRelative utilities` — 29a3182
   - `fix(cli): detect project root, fix bin name, refresh devDep, support update --harness` — d291d6c
   - `chore: add changeset, worklog, and protocol tracking for BUG-01/02` — ed21d83
2. Pushed to `main`. The Release workflow fired but failed on the first attempt: pnpm-lock.yaml still had an `@autonomos/cli` devDep entry that was no longer in the root `package.json`. The entry came from the bug-reproduction session the user ran before the fix; once reverted, the lockfile became inconsistent.
3. Regenerated the lockfile with `pnpm install --no-frozen-lockfile` (which re-aligned it with the cleaned-up `package.json`), amended `ed21d83`, and force-pushed.
4. The workflow re-ran, succeeded, and opened PR #4 ("ci(changesets): version packages") with the version bump + CHANGELOG entries.
5. Squashed and merged PR #4. The release workflow re-ran on the merge commit and published `@autonomos/cli@0.3.1` and `@autonomos/core@0.3.1` to npm. Confirmed via `npm view @autonomos/cli version` → `0.3.1`.

## 📦 What got published

- `@autonomos/cli@0.3.1` (patch)
- `@autonomos/core@0.3.1` (patch)

## ⏭ Next Steps / Context for Successor

- Users who got stuck on `^0.2.0` (which is not on the registry) can now run `pnpm autonomos update` and the devDependency will be bumped automatically to `^0.3.1`, unblocking `pnpm i`.
- The `AUTONOMOS_FORCE=1` / `--force` escape hatch is documented in `autonomos init --help` and works for both interactive and CI contexts.
- **Operational lesson learned** (added to root `AGENT.md`): when the user asks to "adopt the protocol", follow the protocol from the first turn of the session. Crystallizing this lesson is the most valuable thing this session produced — without it, the next session is likely to repeat the same shortcut.

## 🔚 Session End

- BUG-01 [x], BUG-02 [x] in `TASKS.md`.
- Worklog files in place.
- AGENT.md updated with the "follow the protocol from turn 1" rule.
- `manifest.json` updated to `cliVersion: 0.3.1` and `lastUpdated: 2026-06-18T19:04:00Z`.
