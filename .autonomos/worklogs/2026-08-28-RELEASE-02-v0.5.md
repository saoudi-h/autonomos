# WORKLOG: [RELEASE-02] Protocol v0.5 release

**Date:** 2026-08-28
**Agent Status:** [Complete]

## Release

- Published Protocol `0.5.0-alpha`.
- Published `@autonomos/core@0.5.0` to npm.
- Published `@autonomos/cli@0.5.0` to npm.
- Verified the published CLI pins `@autonomos/core` exactly at `0.5.0`.
- Release PR: GitHub #10.
- Publish workflow: GitHub Actions run `33157737219` completed successfully.
- Git tags `@autonomos/core@0.5.0` and `@autonomos/cli@0.5.0` are present.

## Validation

- The published CLI help exposes `init`, `update`, `status`, and the project
  knowledge workflows including `/adopt`.
- A clean fixture initialized with the published CLI created Protocol
  `0.5.0-alpha`, CLI metadata `0.5.0`, and the `/adopt` workflow without
  creating empty `specs/` or `decisions/` directories.
- A fixture initialized with CLI `0.4.1` updated successfully to
  `0.5.0-alpha`; existing `AGENT.md`, `ISSUES.md`, worklogs, and `docs/`
  content was preserved.
- The Core tarball contains `dist/workflows/protocol-adopt.md` and all other
  packaged protocol workflows.

## Notes

- GitHub Actions published the packages using the repository npm token; npm
  authentication is not configured in the local shell.
- GitHub Actions reports the existing non-fatal warning that several upstream
  actions target deprecated Node.js 20 and are temporarily forced onto Node.js
  24 by the runner.

## Files modified

- `.autonomos/manifest.json` records CLI `0.5.0` as the last updater.
- `.autonomos/worklogs/2026-08-28-RELEASE-02-v0.5.md`
